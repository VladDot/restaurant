import { NavBar } from "./navBar";

export const Header = () => {

    return (
        <header className="w-full p-3 bg-blue-600 border-2 border-blue-700">
            <NavBar />
        </header>
    );
};
